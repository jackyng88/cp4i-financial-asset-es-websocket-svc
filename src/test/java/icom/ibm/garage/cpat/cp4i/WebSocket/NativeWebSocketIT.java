package icom.ibm.garage.cpat.cp4i.WebSocket;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeWebSocketIT extends WebSocketTest {

    // Execute the same tests but in native mode.
}